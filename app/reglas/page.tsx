export default function Reglas() {
  return (
    <main className="container">
      <h1>Reglas Oficiales — Aquiservicio (V1)</h1>
      <p style={{ color: "#444" }}>Estas reglas protegen la comunidad y mantienen el sistema útil, profesional y confiable.</p>
      <pre style={{ whiteSpace: "pre-wrap", border: "1px solid #EAEAEA", borderRadius: 16, padding: 16, boxShadow: "0 4px 12px rgba(0,0,0,0.05)" }}>
{`(Aquí pegamos tu documento de reglas completo tal cual lo escribiste, sin editarlo.)`}
      </pre>
    </main>
  );
}
